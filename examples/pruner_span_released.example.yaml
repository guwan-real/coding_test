agent:
  pruner:
    url: http://127.0.0.1:8051/prune
    timeout: 120
    retries: 3
    min_chars: 500
    chunk_overlap_tokens: 50
    threshold: 0.5
    selection_mode: span

# Merge this pruner stanza into the local Mini-SWE-Agent config used for
# SWE-bench rollout. Keep solver API keys in a local ignored config or
# environment variables.
